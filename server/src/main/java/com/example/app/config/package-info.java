/**
 * Spring 配置包。
 *
 * <p>放 {@code @Configuration}、{@code @ConfigurationProperties}、
 * {@code @MapperScan}、MyBatis-Plus 拦截器和基础设施 Bean 装配。配置类只负责
 * 装配，不承载业务流程。</p>
 */
package com.example.app.config;

