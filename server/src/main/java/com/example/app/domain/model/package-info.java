/**
 * 领域模型包。
 *
 * <p>放 MyBatis-Plus Entity、Value Object 和领域枚举。Entity 可使用
 * {@code @TableName}、{@code @TableId}、{@code @TableField} 等持久化映射注解，
 * 但不承载 Controller 或 Service 流程逻辑。</p>
 */
package com.example.app.domain.model;

